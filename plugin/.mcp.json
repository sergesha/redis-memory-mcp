{
  "mcpServers": {
    "redis-memory-mcp": {
      "command": "bash",
      "args": [
        "-c",
        "t=$(mktemp) && curl -fsSL https://raw.githubusercontent.com/sergesha/redis-memory-mcp/main/start.sh -o $t && exec bash $t"
      ],
      "_description": "Self-installing: downloads repo, builds images, starts Redis+TEI, launches MCP server",
      "_required": true,
      "_setup": "Replace YOUR with your GitHub username. Only dependency: Docker."
    }
  }
}